<%= form_for(@music_style) do |f| %>
  <% if @music_style.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@music_style.errors.count, "error") %> prohibited this music_style from being saved:</h2>

      <ul>
      <% @music_style.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
